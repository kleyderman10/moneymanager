VITE_API_BASE_URL=https://moneymanager-e7fa9ac13e3f.herokuapp.com/api
# Product ID of the auto-renewable subscription created in App Store Connect
# (In-App Purchases > Subscriptions). Leave empty until it exists — the Apple
# purchase button stays hidden (isAppleIAPAvailable() returns false) without it.
VITE_APPLE_IAP_PRODUCT_ID=
